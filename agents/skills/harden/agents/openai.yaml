interface:
  display_name: "Harden"
  short_description: "Turn proven exploration into durable code"
  default_prompt: "Use $harden to turn this proven exploration into a clean, durable implementation."
policy:
  allow_implicit_invocation: false
