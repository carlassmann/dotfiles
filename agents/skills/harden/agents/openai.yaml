interface:
  display_name: "Harden"
  short_description: "Turn a proven prototype into durable code"
  default_prompt: "Use $harden to turn this proven prototype into a clean, durable implementation."
policy:
  allow_implicit_invocation: true
